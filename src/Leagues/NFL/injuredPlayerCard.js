/*import styled from 'styled-components/macro'
import getNflInjuryData from '../../Api/fetchDataNfl'

export default function injuredPlayerCard({ player }) {
  const players = getNflInjuryData()
  return (
    <>
      {players.map((player) => (
        <div>
          <p>{player.plyerName}</p>
          <p>{player.team}</p>
          <p>{player.position}</p>
          <p>{player.injury}</p>
          <p>{player.practiceStatus}</p>
          <p>{player.gameStatus}</p>
        </div>
      ))}
    </>
  )
}
*/
